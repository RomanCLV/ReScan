<UserControl x:Class="ReScanVisualizer.Views.ItemTreeViews.AveragePlanTreeViewItemHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReScanVisualizer.Views.ItemTreeViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="Average Plan (" />
        <TextBlock Text="{Binding A, StringFormat=N2}" />
        <TextBlock Text=", " />
        <TextBlock Text="{Binding B, StringFormat=N2}" />
        <TextBlock Text=", " />
        <TextBlock Text="{Binding C, StringFormat=N2}" />
        <TextBlock Text=", " />
        <TextBlock Text="{Binding D, StringFormat=N2}" />
        <TextBlock Text=")" />
    </StackPanel>
</UserControl>

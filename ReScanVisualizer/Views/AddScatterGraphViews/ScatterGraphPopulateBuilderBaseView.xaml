<UserControl x:Class="ReScanVisualizer.Views.AddScatterGraphViews.ScatterGraphPopulateBuilderBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReScanVisualizer.Views.AddScatterGraphViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Foreground="{StaticResource foregroundColor}"
             >
    <StackPanel>
        <TextBlock Text="Choose a method to populate the new graph" />
        <ComboBox Margin="10" Width="200" HorizontalAlignment="Left" SelectedIndex="{Binding PopulateSelectedIndex}">
            <ComboBoxItem Content="Random" />
            <ComboBoxItem Content="Rectangle 2D" />
            <ComboBoxItem Content="Line" />
            <ComboBoxItem Content="Function f(x, y) = z" />
            <ComboBoxItem Content="Parametrics functions : t" />
            <ComboBoxItem Content="Parametrics functions : t1, t2" />
        </ComboBox>
        <Separator Margin="10" />
    </StackPanel>
</UserControl>

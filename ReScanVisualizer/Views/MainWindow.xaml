<Window x:Class="ReScanVisualizer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReScanVisualizer.Views"
        xmlns:itw="clr-namespace:ReScanVisualizer.Views.ItemTreeViews"
        xmlns:viewModels="clr-namespace:ReScanVisualizer.ViewModels"
        xmlns:converters="clr-namespace:ReScanVisualizer.Converters"
        xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        mc:Ignorable="d"
        Title="ReScan Visualizer" Height="720" Width="1280"
        WindowState="Maximized"
        Background="{StaticResource backgroundColor}"
        >
    <Window.Resources>
        <converters:CommandKeyToInputGestureTextConverter x:Key="CommandKeyToInputGestureTextConverter" />
    </Window.Resources>
    
    <Window.InputBindings>
        <KeyBinding x:Name="AddScatterGraphCommandKey" 
                    Command="{Binding Path=AddScatterGraphCommand.Command}"  
                    Key="{Binding Path=AddScatterGraphCommand.Key}" 
                    Modifiers="{Binding Path=AddScatterGraphCommand.Modifiers}"
                    />
        <KeyBinding x:Name="AddBaseCommandKey" 
            Command="{Binding Path=AddBaseCommand.Command}"  
            Key="{Binding Path=AddBaseCommand.Key}" 
            Modifiers="{Binding Path=AddBaseCommand.Modifiers}"
            />

    </Window.InputBindings>

    <DockPanel>
        <StackPanel
            DockPanel.Dock="Top"
            Background="{StaticResource backgroundColor}"
            >
            <Menu DockPanel.Dock="Top" Background="Transparent">
                <MenuItem Header="_Edit" ToolTip="Edit" Style="{StaticResource menuItemTitle}">
                    <MenuItem Header="_Add a graph" ToolTip="{Binding AddScatterGraphCommand.Description}" Style="{StaticResource menuItemItem}"
                              Command="{Binding ElementName=AddScatterGraphCommandKey, Path=Command}"
                              InputGestureText="{Binding ElementName=AddScatterGraphCommandKey, Converter={StaticResource CommandKeyToInputGestureTextConverter}}">
                        <MenuItem.Icon>
                            <Image Source="{StaticResource add}" />
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Header="_Add a base" ToolTip="{Binding AddBaseCommand.Description}" Style="{StaticResource menuItemItem}"
                              Command="{Binding ElementName=AddBaseCommandKey, Path=Command}"
                              InputGestureText="{Binding ElementName=AddBaseCommandKey, Converter={StaticResource CommandKeyToInputGestureTextConverter}}">
                        <MenuItem.Icon>
                            <Image Source="{StaticResource add}" />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
            </Menu>
        </StackPanel>

        <Grid>
            <HelixToolkit:HelixViewport3D ShowCameraInfo="False">
                <HelixToolkit:HelixViewport3D.Background>
                    <LinearGradientBrush StartPoint="0.5 0" EndPoint="0.5 1">
                        <GradientStop Color="#FF0061FF" Offset="0"/>
                        <GradientStop Color="DarkBlue"  Offset="1"/>
                    </LinearGradientBrush>
                </HelixToolkit:HelixViewport3D.Background>
                <!-- Remember to add light to the scene -->
                <HelixToolkit:SunLight/>
                <HelixToolkit:GridLinesVisual3D Width="30" Length="30" MinorDistance="1" MajorDistance="1" Thickness="0.01"/>

                <ModelVisual3D Content="{Binding OriginModel}" />
                <ModelVisual3D Content="{Binding BasesModels}"/>
                <ModelVisual3D Content="{Binding AdditionalModels}"/>
                <ModelVisual3D Content="{Binding Models}"/>
            </HelixToolkit:HelixViewport3D>

            <DockPanel 
                Width="400"
                HorizontalAlignment="Left" 
                >
                <Grid
                    DockPanel.Dock="Bottom"
                    Height="200"
                    >
                    <Grid.Resources>
                        <DataTemplate DataType="{x:Type viewModels:SampleViewModel}">
                            <local:SampleView />
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type viewModels:PlanViewModel}">
                            <local:PlanView />
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type viewModels:BaseViewModel}">
                            <local:BaseView />
                        </DataTemplate>
                    </Grid.Resources>

                    <ContentControl Content="{Binding SelectedViewModel}" />
                </Grid>

                <ScrollViewer 
                    HorizontalScrollBarVisibility="Auto" 
                    VerticalScrollBarVisibility="Auto"
                    >
                    <StackPanel>
                        <TreeView Background="Transparent" BorderThickness="0"
                                  ItemsSource="{Binding Bases, UpdateSourceTrigger=PropertyChanged}">
                            <TreeView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource myTextBlockLeft}" MouseDown="BaseItem_MouseDown" />
                                </DataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                        
                        <TreeView Background="Transparent" BorderThickness="0" 
                                  ItemsSource="{Binding ScatterGraphs, UpdateSourceTrigger=PropertyChanged}">
                            <TreeView.ItemTemplate>
                                <DataTemplate>
                                    <TreeViewItem IsExpanded="True">
                                        <TreeViewItem.Header>
                                            <itw:ScatterGraphTreeViewItemHeader />
                                        </TreeViewItem.Header>
                                        <TreeViewItem>
                                            <TreeViewItem.Header>
                                                <itw:BarycenterTreeViewItemHeader DataContext="{Binding Barycenter, UpdateSourceTrigger=PropertyChanged}" />
                                            </TreeViewItem.Header>
                                        </TreeViewItem>
                                        <TreeViewItem>
                                            <TreeViewItem.Header>
                                                <itw:AveragePlanTreeViewItemHeader DataContext="{Binding AveragePlan, UpdateSourceTrigger=PropertyChanged}" />
                                            </TreeViewItem.Header>
                                        </TreeViewItem>
                                        <TreeViewItem Header="Base (3)" IsExpanded="False">
                                            <TreeViewItem>
                                                <TreeViewItem.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="X ("/>
                                                        <TextBlock Text="{Binding Base3D.X.X, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.X.Y, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.X.Z, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=")"/>
                                                    </StackPanel>
                                                </TreeViewItem.Header>
                                            </TreeViewItem>
                                            <TreeViewItem>
                                                <TreeViewItem.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="Y ("/>
                                                        <TextBlock Text="{Binding Base3D.Y.X, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.Y.Y, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.Y.Z, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=")"/>
                                                    </StackPanel>
                                                </TreeViewItem.Header>
                                            </TreeViewItem>
                                            <TreeViewItem>
                                                <TreeViewItem.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="Z ("/>
                                                        <TextBlock Text="{Binding Base3D.Z.X, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.Z.Y, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=" "/>
                                                        <TextBlock Text="{Binding Base3D.Z.Z, StringFormat=N3, Mode=OneWay}" />
                                                        <TextBlock Text=")"/>
                                                    </StackPanel>
                                                </TreeViewItem.Header>
                                            </TreeViewItem>
                                        </TreeViewItem>
                                        <TreeViewItem ItemsSource="{Binding Samples, UpdateSourceTrigger=PropertyChanged}">
                                            <TreeViewItem.Header>
                                                <itw:SamplesTreeViewItemHeader />
                                            </TreeViewItem.Header>
                                            <TreeViewItem.ItemTemplate>
                                                <DataTemplate>
                                                    <itw:SampleTreeViewItemHeader />
                                                </DataTemplate>
                                            </TreeViewItem.ItemTemplate>
                                        </TreeViewItem>
                                    </TreeViewItem>
                                </DataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Grid>

    </DockPanel>
</Window>
